# 第一阶段实施记录

实施日期：2026-09-18。此记录区分代码完成情况和真实部署验收，不把替身测试作为外部服务已通过的证明。

## 已实现

- 注册、登录、退出、修改密码；用户名统一小写，密码 Argon2 哈希；随机登录凭证通过 HttpOnly Cookie 传递，MongoDB 仅保存凭证摘要。
- 每账号一个默认私有知识库。普通用户可以上传、查询、查看文档清单和下载自己的原文件；管理员无内容读取旁路，尚未加入第二阶段分享/成员权限。
- 聊天、任务、SSE、产品匹配、普通和 HyDE 检索都限定用户及知识库范围；传入模型前复核本地来源。
- 原文件和图片放入新私有桶，按库/文档/任务分开，访问经 `/assets/{id}` 校验归属。回答图片还需属于实际引用的文档；不直接展示外部图片。
- 文档同名替换以库为边界，新版本完整成功后才发布；失败不切换旧的可检索版本。配额预留和任务创建共用锁，失败释放新文档占位。
- 页面账户菜单、分账号聊天恢复、CSRF、401 清理、跨标签退出/账号切换及旧请求隔离。导入状态只影响当前账号；“终止上传”会取消本人仍在处理的导入任务，退出登录仍在账户菜单中。
- 本机账号管理，旧知识/聊天迁移预览、显式应用、归属冲突拒绝、中断恢复和条件回退。真实旧数据尚未迁移。

运行时仅新增 `pwdlib[argon2]`。`mongomock` 属于开发测试依赖，生产可用 `uv sync --locked --no-dev` 排除。

## 验证结果

Python 自动化 **70 项通过**，5 个 Node 前端检查脚本通过；Python 编译检查、依赖锁检查及 Git 差异空白检查通过。

2026-09-19 补充上传终止：取消请求按账号和批次隔离，覆盖接收前取消、接收后取消、排队、备份及发布前取消。正在执行的外部调用返回后停止后续节点，清理本次产物并释放预留；完成的文件保留，登录状态不变。前端测试覆盖终止等待、失败重试和取消上传时的响应竞态；上述新增场景使用服务替身，未调用真实解析或模型服务。

```powershell
uv run python -m unittest discover -s test -p "test_*.py" -v
node test/test_answer_view.cjs
node test/test_session_delete.cjs
node test/test_page_navigation.cjs
node test/test_import_status.cjs
node test/test_auth_view.cjs
uv run python -m compileall -q config utils web processor tools test
uv lock --check
git diff --check
```

新增测试覆盖注册重名和并发唯一性、角色伪造、Cookie 属性、会话过期/闲置/撤销/禁用/改密、Origin/CSRF、限流和数据库故障、跨用户及管理员越权、上传容量、同名隔离、受保护资产、检索范围、未发布版本、迁移恢复和回退。完整 Markdown 导入图与澄清回答图也实际执行，仅在模型和外部存储边界替换为测试实现，验证助手消息每轮只保存一次。

浏览器使用隔离内存数据库和模型/导入替身，验证了：

1. 注册成功后回到登录，新账号可进入业务页。
2. Alice 提问，新会话和流式结果正常显示，返回页面可恢复历史。
3. Alice 选择 Markdown 上传，任务完成后持久文档区域显示文档。
4. 两个标签页登录 Alice，退出其中一个后两个页面均返回登录并清除旧内容。
5. 登录 Bob，文档、任务和会话均为空，看不到 Alice 的内容。
6. 文档页布局可用，浏览器未记录脚本错误。

这些测试没有调用收费模型，也没有向当前 `.env` 指向的业务数据库/对象存储写入账号、文档或迁移记录。临时浏览器验证服务在检查后关闭。

## 启动和剩余部署验证

先对照 `.env.example` 补齐配置，然后：

```powershell
uv sync --locked
uv run uvicorn web.app:app --host 127.0.0.1 --port 8000
```

打开 `http://127.0.0.1:8000/login.html` 注册账号。旧数据默认不进入新账号的知识库；管理与迁移命令见 [运维与迁移](多用户运维与迁移.md)。

当前仍需在真实部署环境完成 MongoDB/Milvus/MinIO/MinerU/模型联调，使用两个普通账号和一个管理员上传同名不同内容文档，核对实际向量查询、私有对象匿名拒绝、图片引用、服务重启后的登录和清单恢复。迁移工具已通过替身测试，正式使用前仍需备份及在数据副本上试运行。

仅支持单 Web 实例、单 Uvicorn worker。任务不续跑；异常中断后释放配额，但存储故障可能留下不可检索的孤立资产/向量。尚无自动垃圾回收、文档删除、共享成员或分布式任务队列。
